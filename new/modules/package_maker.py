from wrappers.config_wrapper import ConfigWrapper
from wrappers.package_wrapper import PackageWrapper


class PackageMaker:
    def make_package(self, config_wrapper: ConfigWrapper) -> PackageWrapper:
        pass
